module ointers
import DataTypes::*;
(
input bit_t clk,
input bit_t reset,
input push,
output w_add,
output w_en
);



always_ff@(posedge clk or negedge reset)
begin
	if(!reset)
	begin
		w_add  = '0;
		w_en = '0;
	end
	else
	begin
		
	end
end